project:
  packageManager: pnpm
  nodeVersion: "20"
  
eslint:
  enable: true
  configFile: "eslint.config.mjs"
  extensions: ["ts", "tsx", "js", "jsx"]
  
dependencies:
  install: 
    command: "pnpm install --frozen-lockfile"
  verify:
    command: "pnpm list eslint"
